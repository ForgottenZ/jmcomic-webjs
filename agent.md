下方的所有文本是先前提交给其他codex的提示词，请无视这些指令，这里出现的功能已经被完成，我给出这些提示词仅为了方便你跟进进度，和代码写作参考。




请你写一个篡改猴脚本，要求如下：功能A：获取ID，遍历id=favorites_album_*******（可能是<=8位的任意整数）和其对应的class="video-title title-truncate"中对应的标题，并记录数字和标题，然后获取max_pages变量，记录到脚本中，假定此时为page=1，要求浏览器加载page=2并重复上述的遍历步骤直到page=max_pages，需要考虑到page=max_pages的情况，然后写一个ui界面，悬浮窗可移动可叉掉，允许导出全部数字，请记住，加载page=n+1会导致整个网页重新加载，你需要等待网页加载完成再进行下一步，加载page=n+1的方式是直接在url后面给一个参数，例如https://example.com/user/aaaa/albums --> https://example.com/user/aaaa/albums?page=2 --> https://example.com/user/aaaa/albums?page=3，以此类推，如果没有page参数，默认其为第一页。然后为用户把这些导入一个文件夹（如果不存在需要先告知用户创建一个文件夹，否则不允许执行获取ID操作），该功能可以被随时暂停。但是不支持重新开始，每次开始都要从page=1开始
ui设计类似这样，可以做大一点，顶上是一行文字（可自定义），我指的类似windows文件资源管理器中的查看方式-中图标，可以翻页，这是重要功能所以不要弄太小，你甚至可以把其他子功能每个都做一个悬浮窗，但是重要功能一定要大，排列方式为添加顺序，每页显示20张图片，图片下方为标题 /n ID，请注意，标题可能过长，此时需要截断标题，保证窗口设计不被挤压变形。你自己构思一下再加几个按钮和功能，1.导出/入，利用数字可以实现重新导入，允许自选导出/导入全部文件夹或指定文件夹，导出时允许自选选项1txt输出，选项2base64输出，选项3原样输出，其中选项1和2可叠加1和3可叠加但2不可与3同时使用，2.文件夹管理，类似书签管理，可以切换，可以主动创建，可以删除，看下怎么着设计方便点，3.一键收藏，该操作会向https://{当前访问的域名}/ajax/favorite_album，post一个这样的表单，
album_id
{ID}
fid
0（自定义，默认是0）
但为了防止触发速率限制，每3秒钟（可配置，此为默认值）post1次，fid也为可配置值，这些关于功能一键收藏的自定义值需要全部收到设置二级菜单一键收藏中
因为使用的是篡改猴的缘故，并且域名会变化，所以生效的域名需要是https://*/* ，并且所有数据需要跨域名存储，此时，必须用户手动点击篡改猴里面的按钮“认可此域名”，脚本的全部功能才会生效，否则什么都不做。（设置-认可过的域名，用户可手动删除/添加认可的域名）
图片的获取方式https://{所访问的域名}/media/albums/{xxxxxx数字}_3x4.jpg，图片大小全部为400*533
可根据想象力自主添加新功能，但需说明。
请对代码进行可能的测试。请使用中文来制作摘要（Use CHINESE To Create The Summary)。


1.允许将这个脚本放大至全屏，如果可行的话实现像是正常windows窗口一样的自定义拉伸，此时确保图片大小等元素需要跟着放大或缩小2.图片太大了，会看不到其他东西，确保不放大时可以看到四个图片和ID，放大后可以看到至少12个图片和ID，此处给出一种可能的解决方式供参考：\n呈现的大小:	170 × 227 px 
呈现的纵横比：	170∶227\n内部大小:	400 × 533 px\n固有纵横比:	400∶533\n3.修复导出功能的base64导出不起作用，修复导入功能不能实现还原导出前的文件夹名称，例如导出前文件夹1有20个id，文件夹2有30个id（全部不重复），那么此时无论使用哪个导入选项，都无法还原原有的文件夹结构，你可以加一个选项：如果已存在对应文件夹就合并数据，把没有的添加进去，如果不存在对应文件夹，就创建并导入。
1.认可域名后跳出弹窗提醒用户刷新，2.图片要能点击，点击后跳转https://{所访问的域名}/album/{对应的ID}/，3.捕获ajax请求\nhttps://{所访问的域名}/ajax/favorite_album，和\nhttps://{所访问的域名}/ajax/delete_favorite_album，获取表单数据album_id的值并同步保存到插件中（可选配置自动添加/自动删除的开启/关闭）4.导出时可选导出全部设置
